@model IEnumerable<Assortie.Models.HistoriquePaiement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Paiement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adherent.Matricule)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Association.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Paiement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adherent.Matricule)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Association.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdHistoriquePaiement }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdHistoriquePaiement }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdHistoriquePaiement })
        </td>
    </tr>
}

</table>
